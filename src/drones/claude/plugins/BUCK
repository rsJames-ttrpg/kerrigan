filegroup(
    name = "creep-discovery",
    srcs = glob(["creep-discovery/**"]) + [
        # Buck2 glob skips dotfiles; enumerate .claude-plugin/ explicitly
        "creep-discovery/.claude-plugin/plugin.json",
    ],
    visibility = ["PUBLIC"],
)

sh_binary(
    name = "install",
    main = "install.sh",
    resources = [":creep-discovery"],
    visibility = ["PUBLIC"],
)
