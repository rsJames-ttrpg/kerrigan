[package]
name = "evolution"
version = "0.1.0"
edition = "2024"

[dependencies]
nydus = { path = "../nydus" }
flate2 = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1"
tracing = "0.1"
